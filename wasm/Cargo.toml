[package]
name = "star-kernel"
version = "0.1.0"
edition = "2021"
description = "Rust/WASM physics kernel for the star system simulation"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
